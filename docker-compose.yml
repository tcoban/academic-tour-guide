version: "3.9"

services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: academic_tour_guide
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/academic_tour_guide
      PORT: 8080
      ROADSHOW_ENV: development
    ports:
      - "8000:8080"
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_BASE_URL: http://localhost:8000/api
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000/api
      PORT: 8080
    ports:
      - "3000:8080"
    depends_on:
      - backend

volumes:
  postgres_data:
